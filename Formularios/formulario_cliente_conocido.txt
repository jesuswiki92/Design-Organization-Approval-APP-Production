<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Project Form</title>
  <style>
    :root{--bg:#f6f8fc;--card:#ffffffee;--line:#dbe4f0;--text:#0f172a;--muted:#5b6b80;--soft:#8b9ab0;--sky:#0ea5e9;--sky2:#2563eb;--rad:24px;--shadow:0 24px 60px rgba(15,23,42,.12)}
    *{box-sizing:border-box}body{margin:0;padding:36px 18px;font-family:"Segoe UI","Helvetica Neue",sans-serif;background:radial-gradient(circle at top left,rgba(14,165,233,.16),transparent 28%),linear-gradient(180deg,#fbfdff 0%,#f6f8fc 100%);color:var(--text)}
    .page{max-width:1080px;margin:0 auto}.hero{display:grid;grid-template-columns:1.15fr .85fr;gap:22px}.card{background:var(--card);border:1px solid var(--line);border-radius:var(--rad);box-shadow:var(--shadow);backdrop-filter:blur(12px)}
    .hero-card{padding:28px;background:linear-gradient(135deg,#fff 0%,#eef6ff 100%)}.eyebrow{display:inline-flex;padding:8px 12px;border-radius:999px;background:rgba(14,165,233,.12);color:#0284c7;font:700 12px/1 "Segoe UI";letter-spacing:.16em;text-transform:uppercase}
    h1{margin:18px 0 10px;font-size:clamp(30px,4vw,44px);line-height:1.03;letter-spacing:-.04em}.copy{margin:0;color:var(--muted);font-size:15px;line-height:1.72}
    .summary{padding:22px}.kicker{margin:0;font-size:12px;font-weight:700;letter-spacing:.18em;text-transform:uppercase;color:var(--soft)}.company{margin:12px 0 0;font-size:25px;font-weight:700;letter-spacing:-.04em}
    .stack{display:grid;gap:12px;margin-top:18px}.pill{border:1px solid var(--line);border-radius:18px;padding:13px 15px;background:#f8fbff}.pill span{display:block;margin-bottom:5px;color:var(--soft);font-size:11px;font-weight:700;letter-spacing:.14em;text-transform:uppercase}.pill strong{font-size:15px}
    .sections{display:grid;gap:20px;margin-top:26px}.section{padding:24px}.title{margin:10px 0 6px;font-size:26px;font-weight:700;letter-spacing:-.04em}.hintcopy{margin:0;color:var(--muted);font-size:14px;line-height:1.62}
    .grid{display:grid;gap:16px}.cols2{grid-template-columns:repeat(2,minmax(0,1fr))}.field{display:grid;gap:8px}.field label{font-size:12px;font-weight:700;letter-spacing:.08em;text-transform:uppercase;color:var(--muted)}
    .hint{margin-top:-3px;color:var(--soft);font-size:12px;line-height:1.5}input,select,textarea{width:100%;padding:14px 15px;border:1px solid var(--line);border-radius:14px;background:#fff;font:inherit;color:var(--text)}
    textarea{min-height:124px;resize:vertical}input:focus,select:focus,textarea:focus{outline:none;border-color:var(--sky);box-shadow:0 0 0 5px rgba(14,165,233,.12)}
    .submit{padding:24px;display:flex;justify-content:space-between;gap:18px;align-items:center}.submit p{margin:10px 0 0;color:var(--muted);font-size:14px;line-height:1.65}
    .btn{border:0;border-radius:999px;padding:15px 22px;background:linear-gradient(135deg,var(--sky) 0%,var(--sky2) 100%);color:#fff;font-size:14px;font-weight:700;letter-spacing:.04em;cursor:pointer;box-shadow:0 16px 30px rgba(37,99,235,.22);transition:opacity .2s}
    .btn:disabled{opacity:.5;cursor:not-allowed}
    .hidden{display:none}
    textarea{width:100%;padding:14px 15px;border:1px solid var(--line);border-radius:14px;background:#fff;font:inherit;color:var(--text);min-height:80px;resize:vertical}
    textarea:focus{outline:none;border-color:var(--sky);box-shadow:0 0 0 5px rgba(14,165,233,.12)}
    .section-done{border-color:#16a34a !important;background:linear-gradient(135deg,#f0fdf4 0%,#dcfce7 100%) !important}
    .section-done input,.section-done select,.section-done textarea{background:#f0fdf4;opacity:.7;pointer-events:none}
    .done-badge{display:inline-flex;align-items:center;gap:8px;padding:8px 16px;border-radius:999px;background:#16a34a;color:#fff;font-size:13px;font-weight:700}
    .confirmation{padding:32px;text-align:center;border-color:#16a34a;background:linear-gradient(135deg,#f0fdf4 0%,#dcfce7 100%)}
    .confirmation h2{font-size:24px;color:#16a34a;margin:12px 0 8px}
    .confirmation p{color:var(--muted);font-size:15px}
    .error-msg{margin-top:8px;padding:10px 14px;border-radius:10px;background:#fef2f2;border:1px solid #fecaca;color:#dc2626;font-size:13px;display:none}
    @media (max-width:880px){.hero,.cols2{grid-template-columns:1fr}}@media (max-width:720px){body{padding:20px 12px}.hero-card,.summary,.section,.submit{padding:22px}}
  </style>
</head>
<body>
  <div class="page">
    <div class="hero">
      <section class="card hero-card">
        <div class="eyebrow">DOA Hub</div>
        <h1>Project Form</h1>
        <p class="copy">Fill in the technical questions so we can define the scope, assess the proposal, and continue with the quotation process.</p>
      </section>
      <aside class="card summary">
        <p class="kicker">Detected client</p>
        <p class="company">{{CLIENT_COMPANY_NAME}}</p>
        <div class="stack">
          <div class="pill"><span>Contact</span><strong>{{CLIENT_CONTACT_FULL_NAME}}</strong></div>
          <div class="pill"><span>Email</span><strong>{{CLIENT_CONTACT_EMAIL}}</strong></div>
          <div class="pill"><span>Reference</span><strong>{{CONSULTA_REFERENCE}}</strong></div>
        </div>
      </aside>
    </div>

    <input type="hidden" id="consulta_id" value="{{CONSULTA_ID}}" />
    <input type="hidden" id="variant" value="{{FORM_VARIANT_KNOWN}}" />
    <input type="hidden" id="client_id" value="{{CLIENT_ID}}" />
    <input type="hidden" id="client_contact_id" value="{{CLIENT_CONTACT_ID}}" />

    <div class="sections" id="sections-container">
      <section class="card section" id="section-aircraft">
        <p class="kicker">Aircraft data</p>
        <div class="title">Aircraft information</div>
        <p class="hintcopy">Provide the aircraft details as they appear on the EASA Type Certificate Data Sheet (TCDS).</p>
        <div class="grid cols2" style="margin-top:18px">
          <div class="field"><label for="tcds_number">TCDS Number</label><input id="tcds_number" name="tcds_number" type="text" required placeholder="e.g. EASA.A.089" /><p class="hint">As per the EASA TCDS.</p></div>
          <div class="field"><label for="aircraft_manufacturer">Manufacturer (as per TCDS)</label><input id="aircraft_manufacturer" name="aircraft_manufacturer" type="text" required placeholder="e.g. Textron Aviation Inc." /></div>
          <div class="field"><label for="aircraft_model">Model (as per TCDS)</label><input id="aircraft_model" name="aircraft_model" type="text" required placeholder="e.g. 208B" /></div>
          <div class="field"><label for="aircraft_count">Number of aircraft</label><input id="aircraft_count" name="aircraft_count" type="number" min="1" step="1" required placeholder="e.g. 2" /></div>
        </div>
        <div class="field" style="margin-top:16px"><label for="aircraft_msn">MSN / Serial numbers</label><input id="aircraft_msn" name="aircraft_msn" type="text" required placeholder="e.g. 208B-1234; 208B-5678" /><p class="hint">Separate multiple serials with ;</p></div>
        <div class="field" style="margin-top:16px">
          <label>TCDS PDF (optional)</label>
          <input type="hidden" name="tcds_pdf_url" id="tcds_pdf_url" />
          <div id="drop-zone" style="border:2px dashed var(--line);border-radius:14px;padding:32px 20px;text-align:center;cursor:pointer;transition:border-color .2s,background .2s;background:#fafbfd">
            <div id="drop-text">
              <p style="font-size:14px;font-weight:600;color:var(--muted);margin-bottom:4px">Drag & drop your TCDS PDF here</p>
              <p style="font-size:12px;color:var(--soft)">or click to browse — PDF only, max 10 MB</p>
            </div>
            <div id="drop-success" style="display:none">
              <p style="font-size:14px;font-weight:600;color:#16a34a" id="drop-filename">File uploaded</p>
              <p style="font-size:12px;color:var(--soft);margin-top:4px">Stored in Supabase. You can replace it by dropping another file.</p>
            </div>
            <div id="drop-loading" style="display:none">
              <p style="font-size:14px;font-weight:600;color:var(--sky)">Uploading...</p>
            </div>
            <div id="drop-error" style="display:none">
              <p style="font-size:14px;font-weight:600;color:#dc2626" id="drop-error-msg">Error</p>
            </div>
          </div>
          <input type="file" id="tcds_file_input" accept=".pdf" style="display:none" />
          <p class="hint">Upload the EASA Type Certificate Data Sheet if available.</p>
        </div>
        <div class="error-msg" id="error-aircraft"></div>
      </section>

      <section class="card section" id="section-technical">
        <p class="kicker">Project Data</p>
        <div class="title">Technical Project Information</div>
        <p class="hintcopy">Complete the technical information so we can assess the project scope.</p>

        <!-- WHAT -->
        <div class="grid cols2" style="margin-top:18px">
          <div class="field">
            <label for="work_type">Work type <span style="color:#ef4444">*</span></label>
            <select id="work_type" name="work_type" required onchange="toggleWorkType()">
              <option value="">Select...</option>
              <option value="proyecto_nuevo">New project</option>
              <option value="modificacion_existente">Modification to existing project</option>
            </select>
          </div>
          <div class="field hidden" id="field-existing-project">
            <label for="existing_project_code">Existing project code</label>
            <input id="existing_project_code" name="existing_project_code" type="text" placeholder="e.g. IM.A.226-0001" />
          </div>
        </div>
        <div class="field" style="margin-top:16px">
          <label for="modification_summary">Modification description <span style="color:#ef4444">*</span></label>
          <textarea id="modification_summary" name="modification_summary" rows="3" required placeholder="Briefly describe the modification you need..."></textarea>
        </div>
        <div class="field" style="margin-top:16px">
          <label for="operational_goal">Operational goal <span style="color:#ef4444">*</span></label>
          <textarea id="operational_goal" name="operational_goal" rows="2" required placeholder="What is the operational goal of this modification?"></textarea>
        </div>

        <!-- WHERE ON THE AIRCRAFT -->
        <div style="margin-top:24px;margin-bottom:12px;padding-bottom:8px;border-bottom:1px solid var(--line)">
          <p style="margin:0;font-size:11px;font-weight:700;letter-spacing:.14em;text-transform:uppercase;color:var(--soft)">Where on the aircraft</p>
        </div>
        <div class="grid cols2">
          <div class="field">
            <label for="impact_location">Where on the aircraft will the modification take place? <span style="color:#ef4444">*</span></label>
            <select id="impact_location" name="impact_location" required>
              <option value="">Select...</option>
              <option value="interior_cabin">Interior &mdash; Passenger cabin</option>
              <option value="exterior_fuselage">Exterior &mdash; Fuselage / external surface</option>
              <option value="cockpit">Cockpit / Flight deck</option>
              <option value="cargo_hold">Cargo hold / Belly</option>
              <option value="wing">Wing</option>
              <option value="empennage">Empennage / Tail</option>
              <option value="engine_area">Engine / Nacelle area</option>
              <option value="landing_gear">Landing gear area</option>
              <option value="other">Other</option>
            </select>
          </div>
          <div class="field">
            <label for="impact_structural_attachment">Does the modification attach to the aircraft structure? <span style="color:#ef4444">*</span></label>
            <p class="hint">Drilling, cutouts, brackets, doublers, or any structural interface</p>
            <select id="impact_structural_attachment" name="impact_structural_attachment" required onchange="toggleStructuralAttachment()">
              <option value="">Select...</option>
              <option value="si">Yes</option>
              <option value="no">No</option>
              <option value="no_se">Not sure</option>
            </select>
          </div>
        </div>
        <div class="field hidden" id="field-structural-interface" style="margin-top:16px">
          <label for="impact_structural_interface">Describe the structural interface</label>
          <textarea id="impact_structural_interface" name="impact_structural_interface" rows="2" placeholder="e.g. External POD mounted on belly, fuselage skin panel, seat tracks..."></textarea>
        </div>

        <!-- WHAT DOES IT INVOLVE -->
        <div style="margin-top:24px;margin-bottom:12px;padding-bottom:8px;border-bottom:1px solid var(--line)">
          <p style="margin:0;font-size:11px;font-weight:700;letter-spacing:.14em;text-transform:uppercase;color:var(--soft)">What does it involve</p>
        </div>
        <div class="grid cols2">
          <div class="field">
            <label for="impact_electrical">Does the modification require electrical connections or wiring? <span style="color:#ef4444">*</span></label>
            <select id="impact_electrical" name="impact_electrical" required>
              <option value="">Select...</option>
              <option value="si">Yes</option>
              <option value="no">No</option>
              <option value="no_se">Not sure</option>
            </select>
          </div>
          <div class="field">
            <label for="impact_avionics">Does it involve avionics or communications equipment? <span style="color:#ef4444">*</span></label>
            <p class="hint">Antennas, transponders, radios, sensors, displays, navigation equipment</p>
            <select id="impact_avionics" name="impact_avionics" required>
              <option value="">Select...</option>
              <option value="si">Yes</option>
              <option value="no">No</option>
              <option value="no_se">Not sure</option>
            </select>
          </div>
        </div>
        <div class="grid cols2" style="margin-top:16px">
          <div class="field">
            <label for="impact_cabin_layout">Does the modification affect the passenger cabin layout? <span style="color:#ef4444">*</span></label>
            <p class="hint">Seats, monuments, galleys, lavatories, stowage compartments</p>
            <select id="impact_cabin_layout" name="impact_cabin_layout" required>
              <option value="">Select...</option>
              <option value="si">Yes</option>
              <option value="no">No</option>
            </select>
          </div>
          <div class="field">
            <label for="impact_pressurized">Is the installation zone pressurized? <span style="color:#ef4444">*</span></label>
            <select id="impact_pressurized" name="impact_pressurized" required>
              <option value="">Select...</option>
              <option value="si">Yes</option>
              <option value="no">No</option>
              <option value="no_se">Not sure</option>
            </select>
          </div>
        </div>
        <div class="field" style="margin-top:16px">
          <label for="impact_operational_change">Will the modification change how the aircraft is operated? <span style="color:#ef4444">*</span></label>
          <p class="hint">New limitations, new procedures, changed dispatch conditions</p>
          <select id="impact_operational_change" name="impact_operational_change" required>
            <option value="">Select...</option>
            <option value="si">Yes</option>
            <option value="no">No</option>
            <option value="no_se">Not sure</option>
          </select>
        </div>

        <!-- WHAT DO YOU HAVE -->
        <div style="margin-top:24px;margin-bottom:12px;padding-bottom:8px;border-bottom:1px solid var(--line)">
          <p style="margin:0;font-size:11px;font-weight:700;letter-spacing:.14em;text-transform:uppercase;color:var(--soft)">What do you have</p>
        </div>
        <div class="grid cols2">
          <div class="field">
            <label for="has_equipment">Do you already have the equipment/component to install? <span style="color:#ef4444">*</span></label>
            <select id="has_equipment" name="has_equipment" required onchange="toggleEquipment()">
              <option value="">Select...</option>
              <option value="si">Yes, I already have it</option>
              <option value="no">No, I need guidance</option>
              <option value="no_aplica">Not applicable</option>
            </select>
          </div>
          <div class="field hidden" id="field-equipment-details">
            <label for="equipment_details">Equipment details</label>
            <textarea id="equipment_details" name="equipment_details" rows="2" placeholder="Manufacturer, model, Part Number, certification (TSO/ETSO)..."></textarea>
          </div>
        </div>
        <div class="grid cols2" style="margin-top:16px">
          <div class="field">
            <label for="has_drawings">Do you have drawings, schematics or technical documentation? <span style="color:#ef4444">*</span></label>
            <select id="has_drawings" name="has_drawings" required>
              <option value="">Select...</option>
              <option value="si">Yes</option>
              <option value="no">No</option>
            </select>
          </div>
          <div class="field">
            <label for="has_previous_mod">Is there a similar previous modification on this or another aircraft? <span style="color:#ef4444">*</span></label>
            <select id="has_previous_mod" name="has_previous_mod" required onchange="togglePreviousMod()">
              <option value="">Select...</option>
              <option value="si">Yes</option>
              <option value="no">No</option>
              <option value="no_seguro">Not sure</option>
            </select>
          </div>
        </div>
        <div class="field hidden" id="field-previous-mod-ref" style="margin-top:16px">
          <label for="previous_mod_ref">Previous modification reference</label>
          <input id="previous_mod_ref" name="previous_mod_ref" type="text" placeholder="STC, SB, project number..." />
        </div>
        <div class="field" style="margin-top:16px">
          <label for="has_manufacturer_docs">Do you have equipment manufacturer documentation? <span style="color:#ef4444">*</span></label>
          <select id="has_manufacturer_docs" name="has_manufacturer_docs" required>
            <option value="">Select...</option>
            <option value="si">Yes (installation manuals, datasheets...)</option>
            <option value="no">No</option>
          </select>
        </div>

        <!-- WHEN -->
        <div style="margin-top:24px;margin-bottom:12px;padding-bottom:8px;border-bottom:1px solid var(--line)">
          <p style="margin:0;font-size:11px;font-weight:700;letter-spacing:.14em;text-transform:uppercase;color:var(--soft)">When</p>
        </div>
        <div class="grid cols2">
          <div class="field">
            <label for="target_date">Desired completion date</label>
            <input id="target_date" name="target_date" type="date" />
          </div>
          <div class="field">
            <label for="is_aog">Is this an AOG (Aircraft on Ground) situation? <span style="color:#ef4444">*</span></label>
            <select id="is_aog" name="is_aog" required>
              <option value="">Select...</option>
              <option value="si">Yes, urgent</option>
              <option value="no">No</option>
            </select>
          </div>
        </div>
        <div class="field" style="margin-top:16px">
          <label for="aircraft_location">Current aircraft location</label>
          <input id="aircraft_location" name="aircraft_location" type="text" placeholder="Airport, city, country" />
        </div>

        <!-- EXTRA -->
        <div style="margin-top:24px;margin-bottom:12px;padding-bottom:8px;border-bottom:1px solid var(--line)">
          <p style="margin:0;font-size:11px;font-weight:700;letter-spacing:.14em;text-transform:uppercase;color:var(--soft)">Extra</p>
        </div>
        <div class="field">
          <label for="additional_notes">Additional information</label>
          <textarea id="additional_notes" name="additional_notes" rows="3" placeholder="Any other relevant information for project assessment..."></textarea>
        </div>
        <div class="error-msg" id="error-technical"></div>
      </section>

      <section class="card submit" id="submit-all">
        <div><p class="kicker">Final step</p><p>Submit all information to complete your request.</p></div>
        <button type="button" class="btn" id="btn-submit-all" onclick="submitAll()">Submit complete form</button>
      </section>
    </div>

    <section class="card confirmation" id="confirmation" style="display:none;margin-top:26px">
      <div style="font-size:48px">&#10003;</div>
      <h2>Form submitted successfully</h2>
      <p>We have received the aircraft and technical project data. We will continue with the evaluation.</p>
    </section>
  </div>

  <script>
  (function(){
    var SB_URL='https://gterzsoapepzozgqbljk.supabase.co';
    var SB_KEY='eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Imd0ZXJ6c29hcGVwem96Z3FibGprIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NzAxOTQzODMsImV4cCI6MjA4NTc3MDM4M30.ImK5L8stHYwolDcYaPG1-RoWs4jJSviLs_0vgXDAGjc';
    var BUCKET='tcds-documents';

    var zone=document.getElementById('drop-zone');
    var fileInput=document.getElementById('tcds_file_input');
    var hiddenUrl=document.getElementById('tcds_pdf_url');
    var textEl=document.getElementById('drop-text');
    var successEl=document.getElementById('drop-success');
    var loadingEl=document.getElementById('drop-loading');
    var errorEl=document.getElementById('drop-error');
    var filenameEl=document.getElementById('drop-filename');
    var errorMsg=document.getElementById('drop-error-msg');

    function showState(s,msg){
      textEl.style.display=s==='idle'?'':'none';
      loadingEl.style.display=s==='loading'?'':'none';
      successEl.style.display=s==='success'?'':'none';
      errorEl.style.display=s==='error'?'':'none';
      if(s==='success')filenameEl.textContent='Uploaded: '+msg;
      if(s==='error')errorMsg.textContent=msg;
    }

    async function uploadFile(file){
      if(!file||file.type!=='application/pdf'){showState('error','Only PDF files are accepted.');return;}
      if(file.size>10*1024*1024){showState('error','File too large (max 10 MB).');return;}
      showState('loading');
      var ts=Date.now();
      var safeName=file.name.replace(/[^a-zA-Z0-9._-]/g,'_');
      var path='tcds/'+ts+'_'+safeName;
      try{
        var r=await fetch(SB_URL+'/storage/v1/object/'+BUCKET+'/'+path,{
          method:'POST',
          headers:{'Authorization':'Bearer '+SB_KEY,'apikey':SB_KEY,'Content-Type':file.type,'x-upsert':'true'},
          body:file
        });
        if(!r.ok){var e=await r.text();throw new Error(e);}
        var publicUrl=SB_URL+'/storage/v1/object/public/'+BUCKET+'/'+path;
        hiddenUrl.value=publicUrl;
        showState('success',file.name);
        zone.style.borderColor='#16a34a';zone.style.background='#f0fdf4';
      }catch(err){
        showState('error','Upload failed: '+err.message);
        zone.style.borderColor='#dc2626';
      }
    }

    zone.addEventListener('click',function(){fileInput.click();});
    fileInput.addEventListener('change',function(){if(fileInput.files[0])uploadFile(fileInput.files[0]);});
    zone.addEventListener('dragover',function(e){e.preventDefault();zone.style.borderColor='var(--sky)';zone.style.background='#eff6ff';});
    zone.addEventListener('dragleave',function(){zone.style.borderColor='var(--line)';zone.style.background='#fafbfd';});
    zone.addEventListener('drop',function(e){
      e.preventDefault();zone.style.borderColor='var(--line)';zone.style.background='#fafbfd';
      if(e.dataTransfer.files[0])uploadFile(e.dataTransfer.files[0]);
    });

    var SUBMIT_URL='https://sswebhook.testn8n.com/webhook/doa-form-submit';

    function postViaIframe(url,data){
      return new Promise(function(resolve){
        var id='_f'+Date.now()+Math.random().toString(36).slice(2,6);
        var iframe=document.createElement('iframe');
        iframe.name=id;iframe.style.display='none';
        document.body.appendChild(iframe);
        var form=document.createElement('form');
        form.method='POST';form.action=url;form.target=id;
        for(var k in data){
          if(data[k]===null||data[k]===undefined)continue;
          var inp=document.createElement('input');
          inp.type='hidden';inp.name=k;inp.value=data[k];
          form.appendChild(inp);
        }
        document.body.appendChild(form);
        setTimeout(function(){
          iframe.onload=function(){
            setTimeout(function(){try{document.body.removeChild(iframe);document.body.removeChild(form);}catch(e){}},500);
            resolve();
          };
          form.submit();
        },100);
      });
    }

    /* --- Toggle conditional fields --- */
    window.toggleWorkType=function(){
      var v=document.getElementById('work_type').value;
      var f=document.getElementById('field-existing-project');
      f.classList.toggle('hidden',v!=='modificacion_existente');
    };
    window.toggleEquipment=function(){
      var v=document.getElementById('has_equipment').value;
      var f=document.getElementById('field-equipment-details');
      f.classList.toggle('hidden',v!=='si');
    };
    window.toggleStructuralAttachment=function(){
      var v=document.getElementById('impact_structural_attachment').value;
      var f=document.getElementById('field-structural-interface');
      f.classList.toggle('hidden',v!=='si');
    };
    window.togglePreviousMod=function(){
      var v=document.getElementById('has_previous_mod').value;
      var f=document.getElementById('field-previous-mod-ref');
      f.classList.toggle('hidden',v!=='si');
    };

    window.submitAll=async function(){
      var btn=document.getElementById('btn-submit-all');
      var errAircraft=document.getElementById('error-aircraft');
      var errTech=document.getElementById('error-technical');
      errAircraft.style.display='none';
      errTech.style.display='none';

      /* Validate aircraft fields */
      var requiredAircraft=['tcds_number','aircraft_manufacturer','aircraft_model','aircraft_count','aircraft_msn'];
      for(var i=0;i<requiredAircraft.length;i++){
        var el=document.getElementById(requiredAircraft[i]);
        if(!el.value.trim()){el.focus();errAircraft.textContent='Please fill in all required aircraft fields.';errAircraft.style.display='block';return;}
      }

      /* Validate technical + impact fields (merged) */
      var requiredTech=['work_type','modification_summary','operational_goal','impact_location','impact_structural_attachment','impact_electrical','impact_avionics','impact_cabin_layout','impact_pressurized','impact_operational_change','has_equipment','has_drawings','has_previous_mod','has_manufacturer_docs','is_aog'];
      for(var i=0;i<requiredTech.length;i++){
        var el=document.getElementById(requiredTech[i]);
        if(!el.value.trim()){el.focus();errTech.textContent='Please fill in all required fields.';errTech.style.display='block';return;}
      }

      var consultaId=document.getElementById('consulta_id').value;

      var aircraftData={
        section:'aircraft',
        consulta_id:consultaId,
        tcds_number:document.getElementById('tcds_number').value.trim(),
        aircraft_manufacturer:document.getElementById('aircraft_manufacturer').value.trim(),
        aircraft_model:document.getElementById('aircraft_model').value.trim(),
        aircraft_count:document.getElementById('aircraft_count').value,
        aircraft_msn:document.getElementById('aircraft_msn').value.trim(),
        tcds_pdf_url:document.getElementById('tcds_pdf_url').value||''
      };

      var techData={
        section:'technical',
        consulta_id:consultaId,
        work_type:document.getElementById('work_type').value,
        existing_project_code:document.getElementById('existing_project_code').value.trim()||'',
        modification_summary:document.getElementById('modification_summary').value.trim(),
        operational_goal:document.getElementById('operational_goal').value.trim(),
        impact_location:document.getElementById('impact_location').value,
        impact_structural_attachment:document.getElementById('impact_structural_attachment').value,
        impact_structural_interface:document.getElementById('impact_structural_interface').value.trim()||'',
        impact_electrical:document.getElementById('impact_electrical').value,
        impact_avionics:document.getElementById('impact_avionics').value,
        impact_cabin_layout:document.getElementById('impact_cabin_layout').value,
        impact_pressurized:document.getElementById('impact_pressurized').value,
        impact_operational_change:document.getElementById('impact_operational_change').value,
        has_equipment:document.getElementById('has_equipment').value,
        equipment_details:document.getElementById('equipment_details').value.trim()||'',
        has_drawings:document.getElementById('has_drawings').value,
        has_previous_mod:document.getElementById('has_previous_mod').value,
        previous_mod_ref:document.getElementById('previous_mod_ref').value.trim()||'',
        has_manufacturer_docs:document.getElementById('has_manufacturer_docs').value,
        target_date:document.getElementById('target_date').value||'',
        is_aog:document.getElementById('is_aog').value,
        aircraft_location:document.getElementById('aircraft_location').value.trim()||'',
        additional_notes:document.getElementById('additional_notes').value.trim()||''
      };

      btn.disabled=true;btn.textContent='Submitting...';
      try{
        await postViaIframe(SUBMIT_URL,aircraftData);
        await new Promise(function(r){setTimeout(r,2000);});
        await postViaIframe(SUBMIT_URL,techData);
        await new Promise(function(r){setTimeout(r,2000);});
        document.getElementById('sections-container').style.display='none';
        document.getElementById('confirmation').style.display='block';
      }catch(err){
        errTech.textContent='Error: '+err.message;errTech.style.display='block';
        btn.disabled=false;btn.textContent='Submit complete form';
      }
    };
  })();
  </script>
</body>
</html>
