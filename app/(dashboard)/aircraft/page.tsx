/**
 * ============================================================================
 * PAGINA SERVIDOR DE AERONAVES (LISTADO) — UI ONLY (frame)
 * ============================================================================
 */

import AircraftPageClient from './AircraftPageClient'

export default function AeronavesPage() {
  return <AircraftPageClient aircraft={[]} />
}
