/**
 * ============================================================================
 * ACCIONES DE SERVIDOR PARA PROYECTOS HISTORICOS — UI ONLY (frame)
 * ============================================================================
 * Stub sin backend. La UI no debe llamar a esta funcion en el frame.
 * ============================================================================
 */

'use server'

export async function deleteProyectoHistorico(_id: string): Promise<{ success: boolean }> {
  return { success: false }
}
