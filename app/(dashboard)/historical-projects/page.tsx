/**
 * ============================================================================
 * PAGINA SERVIDOR DE PROYECTOS HISTORICOS (LISTADO) — UI ONLY (frame)
 * ============================================================================
 */

import HistoricalProjectsPageClient from './HistoricalProjectsPageClient'

export default function ProyectosHistoricoPage() {
  return <HistoricalProjectsPageClient projects={[]} />
}
