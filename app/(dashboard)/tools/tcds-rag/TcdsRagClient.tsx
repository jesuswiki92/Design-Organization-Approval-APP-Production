/**
 * ============================================================================
 * TCDS RAG CLIENT — UI ONLY (frame placeholder)
 * ============================================================================
 *
 * Frame visual del motor de indexacion y busqueda semantica de TCDS.
 * El backend de RAG/AI esta desconectado en esta version frame-only.
 * ============================================================================
 */

'use client'

export function TcdsRagClient() {
  return (
    <div className="rounded-[22px] border border-dashed border-[color:var(--ink-4)] bg-[color:var(--paper)] px-6 py-16 text-center">
      <p className="text-base font-semibold text-[color:var(--ink)]">Próximamente</p>
      <p className="mt-2 text-sm text-[color:var(--ink-3)]">
        El motor de indexacion y busqueda semantica de TCDS esta temporalmente desconectado.
      </p>
    </div>
  )
}
